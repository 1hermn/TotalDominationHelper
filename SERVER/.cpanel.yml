deployment:
  tasks:
    - export REPOPATH=/home/dokaplus/TotalDomHelperServer 
    - export SITEPATH=/home/dokaplus/TotalDomHelperServer 
    - /bin/rsync -rav --exclude '.git' --exclude '.cpanel.yml' --del $REPOPATH $SITEPATH